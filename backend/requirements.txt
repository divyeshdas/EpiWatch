fastapi==0.115.0
uvicorn[standard]==0.30.6
redis[asyncio]==5.1.0
sqlalchemy[asyncio]==2.0.35
asyncpg==0.29.0
alembic==1.13.3
pydantic-settings==2.5.2
python-dotenv==1.0.1

# Geospatial stack — required by ingest.py (osmnx pulls the OSM road network).
# Pinned explicitly so the Docker build is reproducible across platforms.
# geopandas 0.14.x is used rather than 1.0.x because osmnx 1.9.4 predates
# geopandas 1.0 and is tested against the 0.14 API.
osmnx==1.9.4
geopandas==0.14.4
fiona==1.9.6
shapely==2.0.6
pyproj==3.6.1
rtree==1.3.0
networkx==3.4.2
numpy==1.26.4
pandas==2.2.3

pytest==8.3.2
pytest-asyncio==0.24.0
httpx==0.27.2
